version: "3"
services:
  app:
    image: nginx:1.12
    volumes:
      - .:/usr/share/nginx/html/
    expose:
      - "80"
    ports:
      - 8081:80
    container_name: absensi
    networks:
      - app

networks:
  app:
    external:
      name: network_sis
